## PSコントローラーで操作する際は、DS4Windowsなどのゲームパッド入力変換ソフトをご利用ください<br>
DS4WindowsダウンロードURL例：<br>
https://github.com/schmaldeo/DS4Windows/releases<br>
※上記GitHubの公開ソースを利用する場合、Latestをダウンロード→解凍して起動後、PSコントローラーを接続すればUEエディタ上で使用可能になります<br><br><br>


## 実装済み機能

### 基本構成

- Unreal Engine 5.3 / C++主体で実装
- ターン制ローグライクダンジョンRPGの基礎システムを構築
- 共通処理は`Common`、ゲーム固有処理は`Game`配下へ分離
- 入力、グリッド、フロア、ターン、敵、デバッグ描画の基礎機能を実装済み

---

### 入力システム

- Enhanced Inputを使用した入力制御を実装
- `UCmnInputConfig`DataAssetでInputAction / InputMappingContextを管理
- `UCmnInputRouter`で入力モードを管理
  - Game
  - Menu
  - Dialog
  - Disabled
- ゲーム操作とUI操作のInputMappingContext切り替えに対応
- キーボード、ゲームパッド、左スティック、十字キー入力に対応
- カメラ基準の4方向グリッド移動入力へ変換
- 押しっぱなし移動リピート処理を実装
- UI方向入力のリピート処理を実装
- 待機入力を実装
- カメラ回転、ズーム入力を実装
- カメラ上下左右反転設定に対応

---

### グリッドシステム

- グリッド座標とワールド座標の相互変換を実装
- グリッド範囲、床マス、壁マス、階段マスの管理を実装
- Actorの占有情報管理を実装
  - 登録
  - 解除
  - 移動
  - 全クリア
- 通行可能判定を実装
- プレイヤーやエネミーのグリッド座標管理に対応

---

### フロア生成・フロア管理

- DataTableベースのフロア定義を実装
- 固定レイアウトと自動生成レイアウトに対応
- 自動生成フロアでは以下を生成
  - 部屋
  - 通路
  - 床マス
  - 壁マス
  - プレイヤー開始位置
  - 階段位置
- Seed指定による固定生成に対応
- 階段到達時の次フロア遷移を実装
- フロア開始時にグリッド状態、プレイヤー位置、敵配置、ターン数を初期化
- ※フロアのメッシュ自動配置は未実装

---

### エネミー管理

- エネミー基底Actorを実装
- エネミーのグリッド座標管理を実装
- エネミー初期位置の保存とリセットに対応
- エネミー管理Actorで全エネミーの一覧取得とターン行動実行を実装
- 自動生成フロア用のエネミースポーンに対応
- プレイヤー開始位置や階段周辺を避けたスポーン候補抽出を実装
- フロア遷移時に自動生成エネミーを破棄可能

---

### ターンシステム

- ターン管理Actorを実装
- 現在ターン数の保持と更新に対応
- プレイヤー移動または待機後にターンを進行
- ターン進行時にエネミーターンを実行
- フロア開始時にターン数を初期状態へリセット

---

### プレイヤーキャラクター

- グリッドベースのプレイヤー移動を実装
- 壁、範囲外、通行不可マスへの移動制限を実装
- 階段到達時の次フロア遷移に対応
- 待機行動を実装
- スプリングアームとカメラを使用した視点操作を実装
- カメラ回転、ズーム、初期カメラ設定に対応

---

### デバッグ描画システム

- `UCmnDebugWorldSubsystem`でデバッグ描画の全体ON/OFFを一元管理
- カテゴリ単位のデバッグ描画ON/OFFに対応
  - Grid
  - Floor
  - AI
  - Collision
- グリッド状態のデバッグ描画を実装
  - 床マス
  - 壁マス
  - 階段マス
- フロア状態のデバッグ描画を実装
  - 部屋外枠
  - プレイヤー開始位置
  - 階段強調表示
- 常時描画ではログを出さず、手動描画時のみログを出力
- プレイ開始直後はデバッグ描画非表示
- デバッグUIで全体ON/OFFとカテゴリON/OFFを切り替え可能

---

### デバッグUI

- 隠しコマンドで開くデバッグ専用UIを実装
- 表示項目
  - デバッグ描画全体
  - Grid
  - Floor
  - AI
  - Collision
  - 閉じる
- マウスクリック、キーボード、ゲームパッド操作に対応
- UI表示直後の初期フォーカス設定に対応
- 決定入力でフォーカス中の項目を切り替え可能
- 閉じた後はゲーム操作モードへ復帰
- デバッグコマンド入力中は通常ゲーム入力を一時的に抑制
- 入力失敗またはタイムアウト時に通常操作へ復帰

---

## 現在の到達点

現時点では、以下のローグライク基礎ループまで実装済みです。

- フロアを生成する
- プレイヤーを開始位置へ配置する
- 敵を配置する
- プレイヤーがグリッド上を移動する
- 移動または待機でターンが進む
- 敵ターンで敵がプレイヤー方向へ移動する
- 階段に到達すると次フロアへ進む
- デバッグUIからグリッド・フロア描画を確認できる

---

## 未実装・今後の予定


### HP管理

- 共通HP管理Componentを実装予定
- 最大HP、現在HPの管理を実装予定
- ダメージ処理、回復処理、HPリセットを実装予定
- 生存判定、戦闘不能判定を実装予定
 
### 最小戦闘ループ

- プレイヤーとエネミーのHP管理Componentを実装予定
- エネミーのステータスを実装予定
- 敵がプレイヤー隣接時に攻撃する処理を実装予定
- 敵HPが0になったら占有解除して破棄する処理を実装予定
- プレイヤーHPが0になった際の仮ゲームオーバー処理を実装予定

### ゲーム内オプションUI

- カメラ左右反転、上下反転の設定UIを実装予定
- 適用、キャンセル、閉じるボタンの基礎処理を実装予定
- 未保存変更の有無に応じた適用ボタン制御を実装予定

### 設定保存システム

- `UCmnSettingsSubsystem`による共通設定管理機能を実装予定
- `SaveGame`を使用した入力オプション保存機能を実装予定
- カメラ左右反転、上下反転設定の保存機能を実装予定
- Runtime設定値と保存済み設定値を分離予定
- 未保存変更の判定を実装予定
- 設定変更通知用Delegateを実装予定

### 今後の拡張候補

- AIデバッグ描画
- Collisionデバッグ描画
- エネミー行動AIの強化
- 戦闘ログ表示
- ダメージ表示
- アイテム、装備、スキル
- セーブ/ロード本体
- UI全体の本実装
- フロアメッシュ生成
- エフェクト、演出、サウンド
